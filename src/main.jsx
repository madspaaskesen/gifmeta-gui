import React from "react";
import ReactDOM from "react-dom/client";
import GifMetaApp from "./GifMetaApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GifMetaApp />
  </React.StrictMode>,
);
